<%= form_with url: toggle_book_favorites_path(book), method: :get, local: false, class: "favorite_form" do |f| %>
  <%= f.submit "#{ (book.favorited_by?(current_user) ? "♥" : "♡") + book.favorites.count.to_s}", class: "btn btn-sm btn-link" %>
<% end %>
